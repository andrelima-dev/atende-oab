# Backend Atende OAB

API Node.js/Express/Prisma para autenticação e avaliações.

## Scripts removidos
Todos scripts de teste, seed e limpeza foram removidos.

## Como rodar
1. Instale dependências: `npm install`
2. Configure `.env` com o banco
3. Rode: `npm run dev`
